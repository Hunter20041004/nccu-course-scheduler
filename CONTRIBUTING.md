# 貢獻指南

謝謝你願意改善政大排課工具。這是非官方、由社群維護的專案；課程與選課規則仍以政大正式選課系統、系所公告及公開課綱為準。

## 開始之前

需求為 Node.js 22.13 或更新版本。Fork 專案後，從 `main` 建立一個範圍單純的分支（例如 `fix/course-data-070424001`），再執行：

```bash
npm install
npm run dev
```

送出 Pull Request 前請跑完整驗證：

```bash
npm run verify
```

live contract 會連線政大公開系統；若官方端點暫時不可用，請保留失敗訊息並稍後重試，不要為了通過測試放寬正確性斷言。

## 開發方式

本專案採一次一個行為的 **Red → Green → Refactor**：

1. Red：先寫一個會失敗、可重現需求或 bug 的測試，並親眼確認失敗原因正確。
2. Green：只寫足以讓這個測試通過的最少實作。
3. Refactor：在測試保持全綠時整理命名與重複，再開始下一個行為。

請優先測試真實使用流程與資料邊界，不要只斷言靜態文案。涉及政大查詢、課綱或瀏覽器儲存時，除了 mock 測試，也要保留對應的 contract、integration 或瀏覽器測試。

## 課程資料更正需要的證據

課程更正請開啟 Course data correction issue，並提供：

- 適用學期，例如 `115-1`；
- 完整九碼課號與課名；
- 可公開直接開啟的政大官方課綱或正式公告網址；
- 官方資料中支持更正的原文或忠實摘述；
- 預期的固定時段、實體／同步／非同步出席形式；
- 考試、展示或其他固定義務應是衝堂時段，還是只有日期／文字提醒。

已驗證的修正會綁定學期與精確課號，不會因為課名含「TAICA」、「遠距」或「彈性」就套用到其他課或未來學期。若兩份官方資料互相衝突，請同時列出；系統應顯示待確認，不自行猜測。

## 隱私與安全

- **不要提交 API Key**、選課帳密、學生個資、私有截圖或含個人課表的偵錯輸出。
- 請先用假資料重現問題，截圖前遮蔽姓名、學號、信箱與金鑰。
- 發現安全問題時，不要開公開 issue；請依 [SECURITY.md](SECURITY.md) 使用 GitHub Security Advisories 私下回報。

## Pull Request 檢查

請讓每個 PR 只處理一個清楚目的，連結對應 issue，說明 Red／Green 證據、官方來源與剩餘限制。若改到使用者看得到的畫面，附上桌機與手機截圖並說明真實操作流程；審查者會檢查測試、資料證據、隱私邊界與是否意外改變其他學期或課程。
